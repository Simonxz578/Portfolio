# Required before a non-Vercel production deployment so canonical and sitemap URLs are correct.
SITE_URL=https://your-domain.example

# Leave as "/" for Vercel or a custom domain. For a GitHub project site use "/repository-name".
BASE_PATH=/
